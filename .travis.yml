language: c
compiler:
  - clang
  - gcc
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq libcunit1-dev libcunit1 libtool
script: autoreconf -i && ./configure && make && make check
